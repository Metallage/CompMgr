<UserControl x:Class="CompMgr.AddDivisionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompMgr"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*"/>
            <ColumnDefinition Width="0.2*"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" x:Name="NewDivisionTB"/>
        <Button Grid.Column="1" Content="Добавить" Command="ApplicationCommands.New">
            <Button.CommandBindings>
                <CommandBinding Command="ApplicationCommands.New" CanExecute="CommandBinding_CanAddExecute" Executed="CommandBinding_AddExecuted"/>
            </Button.CommandBindings>
        </Button>
    </Grid>
</UserControl>
